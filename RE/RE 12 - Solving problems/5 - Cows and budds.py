import random

def cows_bulls(seed):

    # Generate a number with
    rng = random.Random(seed)
    number = rng.randrange(0000, 10000)
    # inpu
    def inp():
        inp = int(("Something"))
        return inp

    inp()


    # Comparation
    cows = 0
    bulls = 0



    return cows, bulls
seed = int(input())
cows_bulls(seed)
guess = int(imput())
print(guess)
